# NT profile config tool

CLI-приложение для сборки и анализа профилей нагрузочного тестирования из выгрузки статистики.

## Запуск

```bash
python main.py -c examples/configs/config.json -p examples/sources/data.csv -o examples/results/profile.json
```
